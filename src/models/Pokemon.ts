export class Pokemon {
    name: string;
    pokedexNumber: number;

    constructor(name: string, pokedexNumber: number) {
        this.name = name;
        this.pokedexNumber = pokedexNumber;
    }
}